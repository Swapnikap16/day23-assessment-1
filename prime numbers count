#include<stdio.h>
int main()
{
	int n, count=0, i, r, a, b, tot=0;
	scanf("%d%d", &a, &b);
	for(n=a; n<=b; n++)
	{
		count=0;
		for(i=1; i<=n; i++)
		{
			if(n%i==0)
			{
				count++;
			}
		}
		if(count==2)
		{
			printf("%d", n);
			tot++;
		}
	}
	printf("\nNo of primes count=%d", tot);
}
